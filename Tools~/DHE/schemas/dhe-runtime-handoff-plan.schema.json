{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/mofish9/hybridclr-lab/schemas/dhe-runtime-handoff-plan.schema.json",
  "title": "HybridCLR DHE Runtime Handoff Plan",
  "type": "object",
  "additionalProperties": true,
  "required": ["schemaVersion", "format", "baseAssemblySetSha256", "selection", "aotMetadata", "assemblies"],
  "properties": {
    "schemaVersion": { "const": 1 },
    "format": { "const": "hybridclr.dhe-runtime-handoff-plan.json" },
    "baseAssemblySetSha256": { "type": "string", "pattern": "^[0-9a-fA-F]{64}$" },
    "selection": { "const": "embedded-base-metaversion" },
    "aotMetadataManifestSha256": { "type": "string", "pattern": "^$|^[0-9a-fA-F]{64}$" },
    "aotMetadata": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["assemblyName", "sourceKind", "sha256", "manifestSha256", "path"],
        "properties": {
          "assemblyName": { "type": "string", "minLength": 1 },
          "sourceKind": { "enum": ["current-stripped", "fallback-root"] },
          "sha256": { "type": "string", "pattern": "^[0-9a-fA-F]{64}$" },
          "manifestSha256": { "type": "string", "pattern": "^$|^[0-9a-fA-F]{64}$" },
          "path": { "type": "string", "minLength": 1 }
        }
      }
    },
    "assemblies": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["assemblyName", "current", "baseline", "snapshot", "baselineSha256", "currentSha256", "snapshotSha256", "baseMetaVersion", "currentMetaVersion", "baseMetaVersionSha256", "currentMetaVersionSha256"],
        "properties": {
          "assemblyName": { "type": "string", "minLength": 1 },
          "current": { "type": "string", "minLength": 1 },
          "baseline": { "type": "string", "minLength": 1 },
          "snapshot": { "type": "string", "minLength": 1 },
          "baseMetaVersion": { "type": "string", "minLength": 1 },
          "currentMetaVersion": { "type": "string", "minLength": 1 },
          "baselineSha256": { "type": "string", "pattern": "^[0-9a-fA-F]{64}$" },
          "currentSha256": { "type": "string", "pattern": "^[0-9a-fA-F]{64}$" },
          "snapshotSha256": { "type": "string", "pattern": "^[0-9a-fA-F]{64}$" },
          "baseMetaVersionSha256": { "type": "string", "pattern": "^[0-9a-fA-F]{64}$" },
          "currentMetaVersionSha256": { "type": "string", "pattern": "^[0-9a-fA-F]{64}$" }
        }
      }
    }
  }
}
